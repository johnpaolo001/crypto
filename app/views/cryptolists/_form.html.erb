<%= form_with(model: cryptolist, local: true) do |form| %>
  <% if cryptolist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cryptolist.errors.count, "error") %> prohibited this cryptolist from being saved:</h2>

      <ul>
      <% cryptolist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :symbol %>
    <%= form.text_field :symbol, id: :cryptolist_symbol %>
  </div>

  <div class="field">
    <%= form.number_field :user_id, id: :cryptolist_user_id, value: current_user.id, type: "hidden" %>
  </div>

  <div class="field">
    <%= form.label :cost_per %>
    <%= form.text_field :cost_per, id: :cryptolist_cost_per %>
  </div>

  <div class="field">
    <%= form.label :amount_owned %>
    <%= form.text_field :amount_owned, id: :cryptolist_amount_owned %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
